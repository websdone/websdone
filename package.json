{
  "name": "websdone",
  "version": "1.0.0",
  "description": "Websdoen portfolio",
  "main": "index.js",
  "dependencies": {
    "angular-sanitize": "^1.5.7",
    "connect-modrewrite": "^0.9.0"
  },
  "devDependencies": {
    "bower": "^1.3.1",
    "gulp-install": "^0.6.0",
    "http-server": "^0.6.1"
  },
  "css": {
    "sass": {
      "pluginOptions": {
        "sourceComments": true
      }
    }
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
